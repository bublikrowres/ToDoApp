export class Sms {
    public message: string;
    public sendNumber: string;

    constructor(message:string,sendNumber:string) {
        this.message = message,
        this.sendNumber = sendNumber
    }
}
  